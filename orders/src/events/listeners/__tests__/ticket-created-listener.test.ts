import {TicketCreatedListener} from '../ticket-created-listener';
import {natsWrapper} from '../../../__mocks__/nats-wrapper';

const setup = async () => {
    const listener = new Tic
};

it('create and saves a ticker', async () => {

});

it('acks the message', async () => {

})